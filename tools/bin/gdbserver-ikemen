#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/_debug_env.sh"

ADDR="${GDBSERVER_ADDR:-127.0.0.1:2345}"

exec "$DEBUG_ROOT/usr/bin/gdbserver" "$ADDR" "$ROOT_DIR/Ikemen_GO_Linux" "$@"

